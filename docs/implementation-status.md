# PC 后台与后端实现验收清单

目标：根据设计文档，完成全部 PC 后台、后端服务及充电用户小程序完整流程，包括设备导入及各角色日常工作的完整流程。能编译、HTTP 200 或空列表不能单独证明业务完成。

状态：开发中。以下各域均须完成真实持久化、参数校验、鉴权、异常处理、前端交互及对应集成测试，才可标记完成。

钱包退款风控审核新增进展：PC 财务页新增待审队列，授权财务/客服可填写意见通过或拒绝；user 持久审核回执，批准复用原路拆单和预留资金事务，拒绝不占用余额，重复决定幂等。真实数据库测试已验证批准/拒绝、资金预留一次、终态拒绝改签；scripts/test-wallet-risk.ps1 已验证真实 HTTP 队列、客服实时授权和撤销、伪造审核人拦截、分页及幂等拒绝。小程序显示拒绝及审核意见。钱包独立解冻、历史审核查询、推送通知和浏览器完整交互仍待完成。

钱包充值新增进展：补齐原先缺失的小程序充值页面与钱包入口；服务端持久化 UUID、支付单和微信请求快照，取消或网络失败重试使用原订单及原到期时间，支持缓存预支付参数、账号隔离和充值记录分页。开发库已应用 user_db/0015。真实 MySQL/Redis 测试验证不可达支付渠道两次重试不新增订单、缓存支付参数、金额/账号/微信身份冲突、过期和已支付门控及列表隔离；小程序 40 项测试通过。真实微信商户支付、真机页面验收、汇付渠道和客户自定义充值限额尚未完成。

人工退款申请新增进展：订单详情对授权财务账号显示申请入口，金额和依据提交后作为第一签；本地保存 UUID 以恢复不确定请求，服务端幂等键绑定账号/订单/金额/原因。服务端要求终态充电订单、有效微信支付和未超出的可退额度，申请记录与第一签同事务落库，第二签前不产生支付事件。scripts/test-manual-refund.ps1 已验证实时权限、非终态拒绝、相同请求重放、修改请求拒绝、已有待退款额度计入上限、失败回滚和首签门控；后端编译与前端类型检查通过。人工申请弹窗本身的浏览器验收、钱包风控审批及消息通知仍待完成。

退款页面浏览器验收：本地 PC 后台使用临时 customer_finance 账号和虚构支付/退款记录，已验证权限不足提示、空审核意见拦截、第一签提交后显示等待第二签且同账号不能再签、拒绝后保留原因与终态、退款单号及待处理/已拒绝筛选。发现并修复 HTTP 错误只显示英文状态码及退款列宽挤压问题；后台错误信息增加权限/业务冲突说明和网络/超时提示，5xx 不展示内部错误。前端 7 项测试与类型检查通过。临时记录、账号及权限关联已清理；浏览器第二签和实际微信资金操作未在本轮验证。

审核拒绝新增进展：待第二签、未进入执行的充电退款可由有效财务账号填写原因后拒绝；回执、状态和时间线同事务保存，原退款不能继续审批或执行。拒绝金额从可退额度占用中释放。scripts/test-refund-review.ps1 -Reject 已验证幂等拒绝、审批/执行拦截、不生成执行事件，以及后续全额退款审核不被旧拒绝金额阻挡。后台已提供拒绝入口和 rejected 筛选；钱包风控拒绝、消息通知和真实浏览器验收仍待完成。

双签审核新增进展：终态充电订单的 pending 退款支持两名 customer_finance 账号分别提交审核；当前权限为 order.refund.review。第一签与快照持久化，第二签及执行事件同事务提交，执行入口再次核验快照；PC 列表显示审核状态和意见，授权账号可以提交。scripts/test-refund-review.ps1 已验证重复签署、同账号不能完成双签、快照变更、第一签停用、单次执行事件及执行门控；前端类型检查通过。尚缺人工退款申请创建、审核拒绝、钱包风控审核及消息通知；尚未做真实微信资金和浏览器完整交互验收。

近期退款查询进展：PC 财务退款页已改为真实 user 服务查询，支持分页、状态和完整单号筛选、故障提示及重试。新增 `finance.refund.read` 权限点（需由管理员分配给授权角色），撤销权限立即生效。`scripts/test-refund-list.ps1` 已验证真实金额、筛选、分页、软删除、非法参数、权限授予与撤销；PC TypeScript 检查通过。双签审批、风险冻结审核和异常款人工处置仍未完成，不能据此认定财务流程完成。

异常自动退款恢复已接入现有 refund_task：PC 显示任务错误，并在可重试且具备 `finance.refund.retry` 时允许填写原因后唤醒原任务。调度与审计同事务提交，保留请求/结果快照及处理阶段；不会将 done/manual_review 重新置为 queued。真实 HTTP/MySQL 测试已验证权限、原因校验、任务缺失、调度、快照不变、审计及终态拒绝。待人工审核任务仍需后续双签流程；此处只处理已有自动任务的异常恢复。

## 按角色贯通验收

| 角色 | 必须贯通的日常流程 | 当前证据与缺口 |
| --- | --- | --- |
| 充电用户（小程序） | 授权登录与协议同意 → 找桩/扫码 → 选择端口并展示价格 → 微信/余额/会员卡支付 → 设备确认启动 → 实时充电/主动停止 → 费用结算/退款 → 历史订单、发票、评价、报障、客服；余额充值、优惠券和会员卡管理 | 当前只有 index、charging、profile 三组页面；其余流程及完整异常补偿尚待实现和端到端验证 |
| 运营人员 | 站点配置 → 厂商/设备录入与批量导入 → 端口与二维码 → 费率/分账模板绑定 → 日常监控、订单排查、优惠券/会员卡/公告管理 | 设备页当前仅有列表；设备导入与错误行报告、重复处理、gateway 注册同步、审计均未完成 |
| 财务人员 | 账单核对 → 分账明细/汇总 → 提现审核与打款记录 → 双签退款 → 发票审核 → 对账差异处理 → 导出 | 订单分账只读已接入；各项财务写流程及权限隔离仍需逐项实现 |
| 巡检人员 | 设备状态/故障定位 → 告警确认 → 现场处理记录 → 恢复/关闭 → 查看历史 | 告警处置和可追溯处理流程尚未完整验收；不另建工单系统 |
| 客服坐席 | 微信原生客服接待 → 查询用户反馈/评价/投诉 → 回复与人工跟进 → 重大投诉关联告警 | 微信会话依赖配置与真实环境；后台留存、权限、脱敏和处理记录仍待完成 |
| 客户管理员 | 初始配置 → 账号与预置角色 → 功能/数据/字段权限 → 白标、客服坐席、SLA → 审计、Webhook、OTA | 不能以超级管理员验证替代各角色验收；每条流程须验证授权成功及越权被拒绝 |

上述表是全量目标的验收入口，不能用单个接口或局部订单页面通过代替角色流程通过。

| 业务域 | 依据 | 当前缺口与验收要求 |
| --- | --- | --- |
| 认证与账号 | api/admin.md A；需求 §4、§11 | 登录锁定、刷新令牌轮换/撤销、改密、个人信息、账号 CRUD、解锁、审计 |
| 角色与权限 | api/admin.md B；需求 §11.2 | 预置角色、操作权限、菜单权限、数据范围、字段脱敏；服务端拒绝越权 |
| 站点、厂商、设备 | api/admin.md C；api/gateway.md | 持久化 CRUD、筛选、关联限制、遥测/会话/故障详情、实时状态 |
| 订单查询 | api/admin.md D；api/user.md 历史/详情 | 真实跨服务分页、筛选、价费分离、分账详情、持久化事件时间线；禁止吞错为空列表 |
| 告警与风控 | api/admin.md E；需求 §10 | 规则、订阅、确认/关闭、限流阈值、触发及恢复、通知 |
| 财务审核 | api/admin.md F；api/billing.md | 分账单、双签退款、发票审核、对账及差异处理 |
| 优惠券与会员 | api/admin.md G；api/user.md | 模板、发放/使用统计、核销、有效期、并发幂等 |
| 公告、白标、客服、投诉 | api/admin.md H；需求 §5.8–5.10、§11 | 完整配置与发布、有效期、品牌应用、客服配置、反馈处理 |
| Webhook/监管推送 | api/admin.md I；需求 §10、§12 | 订阅、HMAC、SSRF 防护、投递、幂等重试、日志、人工重试 |
| OTA | api/admin.md J；api/gateway.md；api/worker.md | 固件校验、计划、执行、灰度、设备 ACK、双触发失败检测 |
| 计费与分账模板 | api/admin.md K；api/billing.md | 价费分离、分时规则、版本快照、总比例校验、整数舍入、幂等结算 |
| 审计与导出 | api/admin.md L；api/worker.md | 所有写操作审计、只读查询、导出任务、权限检查、过期下载 |
| 仪表盘 | 需求 §5；PC 后台 | 替换静态零值，真实指标、时间区间、趋势和异常状态 |
| 支付与充电闭环 | api/user.md；技术规格 §5 | 微信验签/解密、支付幂等、端口独占、设备 ACK 后启动、停止、退款与补偿 |
| TCP/MQTT 设备接入 | api/gateway.md；技术规格 §6 | 厂商适配、真实 MQTT、认证、心跳、指令 ACK、离线补传、遥测聚合 |
| 事件可靠性 | 技术规格 §5；api/worker.md | Outbox、PEL 恢复、消费者幂等、退避、DLQ 与人工重放 |
| 定时任务与运维接口 | api/worker.md | 文档任务码、任务记录/触发、跨服务访问、归档、对账、过期清理 |
| 公共基础与交付 | 技术规格 §7–§15 | 统一契约/错误、真实健康检查、迁移、OpenAPI、限流、日志指标、备份恢复、CI、部署验收 |

## 文档冲突处理

- `api/user.md` 的 P1-7 修正、`db/user.md` 和实际迁移均将 `charge_order` 放在 `user_db`；`api/admin.md` 的部分段落仍称其在 gateway，技术规格部分段落又称 billing。采用明确的 P1-7 修正：user 保存充电生命周期，billing 保存费用和分账，gateway 提供设备数据。禁止增加跨 schema SQL 来掩盖归属冲突。对外后台订单接口保持不变，内部查询由数据所有者提供。
- 现有运行环境使用 `X-Service-Token`，文档部分写 `Authorization: Bearer`。新客户端暂沿用现有内部鉴权，统一迁移与兼容测试仍列为公共基础待办。
- 数据库 SQL 与部分文档列名不一致；实现按已部署 schema 明确映射 API 字段，并随功能更新文档，不能用 `unwrap_or_default` 隐藏 SQL/反序列化错误。

## 已验证的基础修复

- Redis 消费者异步注册：两个真实 Redis 集成测试通过；五个后端健康接口返回 200。
- JWT 中间件与身份提取器兼容：三项回归测试通过；有效管理员可连续访问订单路由，未登录仍返回 401。

上述基础修复不代表业务域完成。每批新增实现须补充实际验证证据及剩余缺口。

## 2026-09-26 订单查询进展

- 已实现 user 所有的真实分页查询、时间/设备/状态筛选、admin 站点筛选及权限检查；前端包含筛选、分页、详情抽屉和失败重试。
- 详情通过内部 HTTP 读取 billing 费用及分账快照，展示参与方比例、分账金额、状态；最新计费结果优先于 user 旧费用快照。无记录与查询失败分别处理。
- `scripts/test-orders.ps1` 真实 MySQL/HTTP 集成测试通过，覆盖分页、软删除、校验、权限、支付退款、未计费、费用来源及两方分账；临时数据清理通过。
- admin-web 生产构建及 `cargo check --locked -p billing -p admin -p user` 通过。尚未完成本批浏览器视觉验收。
- 仍待完成：持久化事件时间线、订单数据范围权限、查询限流、列表费用快照同步，以及计费/分账写入闭环的完整实现与验收。上述查询进展不代表订单域或全局目标完成。

### 订单事件记录增量

- 新增 `user_db/0002_charge_event_log.sql`；本地已有数据库已应用，空库初始化按文件顺序执行全部迁移。其他已有环境需要先应用该增量 SQL，再部署依赖事件表的 user/admin 服务。
- 创建、取消、支付确认、设备启动结果与事件记录在同一事务内提交。设备重复启动结果不再重复改写状态；冲突结果返回 409。
- admin 时间线端点校验 `order.read`，user 排除软删除订单，按发生时间和主键稳定排序；前端订单详情展示实际事件。
- 真实接口测试已覆盖时间排序、软删除、越权、重复失败结果只写一条事件。完整事件链仍缺充电结束、结算、退款以及可靠事件消费；创建/支付外部闭环和浏览器验收尚未完成。

### 设备导入基础能力

- 新增 gateway 设备批量建档接口及共享 DTO，校验数量、ID、重复行、厂商和端口数。设备与端口、幂等记录同事务；重试不重建设备，冲突不覆盖配置。
- 新增 gateway_db/0002_device_provision.sql，本地已应用；独立非空主键解决导入请求之间的并发去重，未声称修复其他遗留写入口的可空唯一索引。
- `scripts/test-device-provision.ps1` 验证真实 HTTP/MySQL 创建、串行与并发重试、冲突、批次回滚、扫码解析和内部鉴权；共享契约测试通过。
- 尚未完成：PC 文件导入/预览/错误反馈、admin 站点与数据权限校验、元数据同步的持久化重试、审计、厂商管理及设备真实上线验收。设备导入全流程仍不能标记完成。

### 后台导入流程增量

- PC 设备页新增 UTF-8 CSV 模板、文件选择、校验预览、确认导入、最近 50 条个人导入记录及失败重试入口。
- admin 新增 device.import 功能权限检查、站点存在性检查、持久化导入请求、设备幂等保护；成功同步 gateway 后同事务提交后台元数据、完成状态及审计。失败保留错误，可在重启后人工重试。
- `admin_db/0002_device_import.sql` 已在本地应用。开发账户种子增加导入权限；生产角色授权仍待统一实现。
- 扩展真实集成测试通过：后台导入产生两侧设备、重复完成不重复审计、无效站点保留失败记录且无部分设备。前端构建通过。
- 仍待完成：浏览器操作验收、CSV 更全面测试、数据范围权限、后台自动重试、厂商管理及真实设备上线。当前版本使用厂商和站点 ID，尚未提供可选项查询和完整运营引导。

### 导入页面浏览器验收

- 已通过实际浏览器选择本地 CSV → 核对预览 → 确认导入 → 记录显示已完成 → 设备列表显示新设备的流程；临时设备、站点、厂商与审计数据已清理。
- 修复错误引号被静默改写、空行后错误行号不准确、非十进制数字被接受、重复选择同一文件无响应的问题。浏览器验证错误 CSV 显示具体行号并禁止提交。
- `admin-web npm test` 的 5 项 CSV 测试及生产构建通过，覆盖 BOM/CRLF、逗号/转义引号、坏引号、实际行号、重复设备、数值边界、控制字符和批次限制。测试运行需支持原生 TypeScript 加载的 Node 版本；开发容器为 Node 24。
- 仍需完成失败重试的浏览器验收、权限范围、自动同步重试、厂商配置和实际设备上线。局部导入验收不代表全部运营工作流程已完成。

### 导入自动恢复

- 新增 `admin_db/0003_device_import_retry.sql`，本地已应用。admin 每 5 秒恢复到期导入任务，临时服务故障限次退避，非法参数/冲突/撤销权限停止自动执行。所有尝试重新检查原操作员当前权限。
- 失败时回滚元数据到事务保存点，在持有任务锁时更新失败记录，避免并发失败覆盖完成状态。
- 单元测试验证退避和停止条件；真实集成测试模拟 gateway 已成功、admin 尚未提交的持久化任务，自动恢复后两侧各一条设备且审计完成。停用账号任务自动转为不可重试失败，gateway 无新设备。测试数据已清理。
- 自动恢复尚未经过真实断网/进程强杀及多实例负载测试；数据范围权限、厂商配置、完整用户充电闭环和其他角色业务仍未完成。

### 设备上线注册边界

- 删除注册接口中直接插入/覆盖设备的旧路径。注册现在仅接受预先建档且启用的设备，核对厂商、端口数及提供的站点/型号，并检查厂商状态和传输类型。
- 设备行锁保护会话更新：关闭旧会话、创建新会话、更新最近在线时间在同一事务提交；Redis 会话缓存失败不回滚数据库。
- TCP 演示连接禁止中途切换设备身份；处理失败返回错误，不再回成功 ACK。
- gateway 构建及真实 HTTP/MySQL 回归通过，覆盖重复注册不重复设备、旧会话关闭、未知/停用设备拒绝。TCP 身份切换也纳入集成脚本。
- 仍有明确缺口：注册请求目前保留既有 vendor_id/port_count 契约，尚未落实文档 vendor_code/protocol_version、会话令牌、私有协议版本协商及 TCP 注册会话一体化。厂商适配器和真正 MQTT 尚未实现，已向用户询问首批厂商型号与协议版本；其他模块继续推进，不将未知厂商能力标为完成。

### 充电用户订单流程

- 小程序新增我的订单、订单详情页面及个人中心入口；支持状态筛选、分页、登录提示、错误重试、刷新及防止旧请求覆盖当前页面。
- 用户历史查询增加总数、状态过滤、软删除过滤、严格分页参数校验；详情支持数值 ID 与原订单号，查询 SQL 绑定 JWT 用户 ID 后才访问 billing。用户响应不包含运营方分账参与者。
- 详情费用以 billing 最新结果为准；支付与退款摘要从 user 自有数据库读取。用户列表不再局限后台默认七天窗口。
- 真实接口测试验证本人查询、其他用户 404、软删除 404、计费结果与支付金额及无分账信息泄露。Node 页面逻辑测试 4 项、user Rust 单测 6 项通过。
- 尚未完成：站点名称关联、用户列表费用快照同步、文档其余详情字段、曲线/评价/发票链路，以及微信开发者工具实机验收。app.json 仍声明多个缺失页面，整包小程序尚不可标记可交付；不以页面逻辑测试替代微信运行验收。

### 用户找桩与站点详情

- 修复 user 未转发经纬度、未替换详情路径参数及吞下游错误的问题。admin 只返回开放且未删除站点，显式解码 DECIMAL 坐标，修复详情经纬度对调。
- 查询采用球面距离，按距离过滤并排序后限制最多 100 条，验证经纬度范围和 0.1–50 公里搜索半径。
- 新增小程序附近站点与详情页，包含用户主动定位、半径选择、地图标记、距离列表、导航和站点电话。定位失败、未登录及请求失败有明确反馈。
- 真实接口测试通过，覆盖经纬度转发、详情坐标、禁用站点排除、跨日界线距离与排序；参数单测和页面语法检查通过。
- 仍需完成空闲端口/在线状态、价格与设备摘要、微信地图实机验收、站点坐标系统一及小程序其余缺失页面。不可把站点基本信息查询当作完整找桩到充电闭环。

### 扫码读取契约修复（2026-09-26）

- gateway 扫码解析和端口详情统一使用印刷端口码字符串作为 `port_id`，设备扫码按端口号返回类型化列表。删除端口不可见，已停用/删除设备及厂商不可扫描；重复端口数据明确报冲突。
- user 扫码接口使用严格内部客户端，解包下游 envelope 后返回一次 envelope，404 保留为资源不存在，内部认证失败不清除用户登录。端口详情不再暴露其他用户的 `current_order_id`。
- 此处只实现只读设备/端口识别，未宣称在线可用性、价格、占用锁、支付和启动闭环已完成；小程序扫码与确认页仍待接入。
- 验证：gateway/user 的 cargo check --locked 通过；scripts/test-device-provision.ps1 通过，新增用户 JWT 转发、单层响应、端口码一致性、404、删除端口和禁用设备断言。

### 支付前订单数据修复（2026-09-26）

- scan/start 在预占端口前读取 gateway 的真实端口信息和 billing 报价，拒绝不存在/非空闲端口；优惠券抵扣尚未实现时明确报错，避免忽略选择后按原价支付。
- 修复原来 device_id=port_id、port_no=1 和 payment_order.total_cents=0 的写入错误。充电订单、支付订单关联及创建事件在同一事务内保存，预付款与最终计量费用分开。
- 验证报价总额为正、分项非负且求和一致、未溢出微信整数金额范围；事务写入失败释放自己的预占，提交后或提交结果不明则保留预占，等待恢复/到期。支付订单过期时间、微信请求和前端返回共用同一截止时刻。
- 单元测试与真实 MySQL 事务测试通过，覆盖金额溢出/不一致、7 号端口身份、非零支付额、订单关联、事件及回滚。
- 尚待完成：实际站点计费规则（当前 billing quote 仍为默认规则）、在线/实时占用复核、微信 RSA 签名和验签、支付创建失败恢复、取消/过期恢复和幂等重试。此改动不代表支付闭环可验收。

### 待支付取消校验与预占释放（2026-09-26）

- 取消接口按支付回调相同顺序锁定支付单、充电订单，校验用户归属、软删除、双向关联、两张单据状态及 60 秒窗口；不再只看充电订单而覆盖已支付单据，也不再用当月条件错误过滤跨月订单。
- 本地取消同时写 charge_order.cancelled / payment_order.closed 及一次取消事件。重放取消返回成功并重试 Redis CAS 释放，仅删除 `user_id:order_no` 匹配的预占，保留后来订单的锁。
- 真实 MySQL 测试覆盖他人订单、已支付单据、超时、重复取消及事件幂等；scripts/test-orders.ps1 真实 HTTP/Redis 回归通过，验证首次释放和后续预占不被误删。
- 未完成项：微信远程关单、取消先提交后迟到支付的退款补偿、可靠释放恢复任务。当前支付回调仍是待替换实现，不能据此宣称取消与真实支付的完整竞争处理已完成。

### 微信登录账户创建修复（2026-09-26）

- 修复 login 对 INSERT 使用 fetch_one 导致取不到用户 ID 的错误；新增 user_login_identity 主键锁表，按大小写精确的 OpenID 串行首次创建，避免旧 nullable 组合唯一键造成并发重复账户。迁移 0003 已应用本地开发库，新库初始化自动执行。
- is_new_user 根据实际创建分支返回，不再用最近 10 秒注册时间猜测。保存微信返回的 unionid；遇到重复历史账户或关联冲突报错，不自动合并、复活已删除账户或解冻账户。刷新现有 JWT 时检查用户当前状态及身份匹配。
- code2Session 使用结构化查询参数和有限超时，不把包含 app secret 的请求 URL 放进错误；兼容成功响应缺少 errcode/errmsg、失败响应缺少 openid，拒绝成功码但身份字段缺失的响应。
- 真实 MySQL 并发测试通过：同 OpenID 同时登录只有一次新建，后续登录沿用用户 ID，冻结/删除账户拒绝登录。微信响应解析单测通过。
- 仍待完成：微信真实 code 联调、Refresh Token 独立轮换与撤销、所有已签发令牌的实时禁用检查、登录审计及完整认证验收。

### 用户刷新令牌与小程序自动续期（2026-09-26）

- 登录返回独立随机 refresh_token（Redis 保存、7 天有效）及 jwt_expires_in；公开 refresh 改为 Authorization: Bearer RT_...，原访问 JWT 不再用于续期。轮换通过 Lua 原子消费旧令牌、保存新令牌，同令牌并发请求只有一次成功。
- refresh 查询当前账户状态，冻结/删除账户拒绝刷新并撤销该刷新令牌。新增 POST /api/v1/public/auth/logout，撤销提交的刷新令牌，重复调用可成功。
- 小程序保存刷新令牌，访问接口明确返回 401 时共用一个刷新请求并重试一次；403/业务失败不触发刷新，网络错误保留会话。退出等待本客户端在途刷新，成功撤销后清空本地会话；退出请求失败可重试，不假报服务端撤销成功。
- 验证：真实 Redis 并发测试、scripts/test-user-session.ps1 HTTP+DB+Redis 回归、小程序 session.test.cjs 3 项测试通过，覆盖轮换/重放/访问 JWT 拒绝/退出/冻结/并发续期/断网。
- 限制：退出当前只撤销刷新令牌，已签发访问 JWT 在过期前仍有效；会话族整体撤销和所有用户路由实时状态检查仍需补齐。JWT 时长沿用服务配置，尚未统一为设计的 15 分钟；微信实机联调尚未完成。

### 用户会话即时撤销与实时账户状态（2026-09-26）

- 用户 JWT 增加 sid，新登录及刷新签发的访问令牌固定 15 分钟。所有 /user 路由统一校验 Redis 会话存在、用户 ID/OpenID 匹配及数据库 active/未删除状态；无 sid 的旧用户令牌需重新登录，内部服务和后台认证不受此接口变更影响。
- Redis 维护同一登录会话当前刷新令牌。轮换保留旧令牌的会话关联直至原 TTL，只允许当前刷新令牌续期；退出可凭该会话已轮换的旧令牌撤销整组会话，解决退出与刷新竞争时漏撤销新令牌的问题。历史刷新令牌不会重新获得刷新能力。
- 实时 HTTP 回归通过：访问令牌可读取本人订单，使用刷新前的旧令牌退出后该访问令牌立即 401；冻结账户现有访问令牌立即 403，刷新拒绝并撤销会话。Redis 并发轮换测试通过；订单及设备集成测试使用真实测试账户和独立会话。
- 仍需微信实机登录与支付联调，以及用户身份审计、请求限流等设计验收项；这轮不代表全量目标已完成。

### 个人中心资料与登录状态（2026-09-26）

- /user/profile 改为类型化响应，在同一事务读取资料、未删除钱包、有效未使用优惠券数量及当前有效会员卡；返回注册时间、手机号是否已保存加密信息、可用/冻结余额，不返回 OpenID、电话明文或哈希。余额来自当前 schema 的 balance_cents（对应可用余额），保留旧 balance_cents 字段供现有调用兼容。
- 查询失败不再吞成零余额；钱包重复明确 409。已过期/使用/删除/禁用模板优惠券不计入可用数量，只有有效期内 active 会员卡返回。
- 个人中心未登录时提供直接微信登录；加载、错误、重试可见，资金固定两位小数，移除不存在的默认头像资源。隐藏页面或退出后迟到的资料请求不能恢复旧用户资料。
- scripts/test-user-session.ps1 真实接口新增个人中心断言通过；小程序 10 项测试通过，含 3 项个人中心异步状态测试。未完成微信开发者工具/实机视觉验收，手机号绑定、优惠券使用、钱包资金与会员交易仍需继续开发。

### 钱包余额与资金流水读取（2026-09-26）

- 钱包余额接口返回可用金额、冻结金额及状态，过滤删除账户；不存在账户为零，查询失败不伪装零余额，重复账户明确报冲突。
- 流水接口按 JWT 用户归属查询，支持设计中的 type 筛选（consume→pay、admin_adjust→adjust），时间/id 倒序稳定分页、总数及严格分页校验；返回带正负号的 amount_cents、txn_type、remark 和 RFC3339 时间，数据库解码错误不再用零值替代。
- 新增小程序钱包主页和流水页，支持登录、两位金额、冻结提示、类型选择、加载更多及失败重试；退出后在途读取不会回填旧流水。
- scripts/test-user-session.ps1 真实接口覆盖资金数据、消费筛选、分页、其他用户隔离及非法参数；小程序 13 项测试通过，含新增 3 项钱包测试。未完成微信实机视觉验收。
- 充值页/支付、余额消费、原路退款及对账闭环仍需继续开发；钱包写接口原有占位实现尚未完成，不能视为钱包全功能验收通过。

### PC 站点维护阻断修复（2026-09-26）

- 修复新建站点 INSERT/fetch_one 错误，改用 last_insert_id；修复 DECIMAL 经纬度读取为 0/详情解码失败。新增经纬度范围、文本长度/控制字符和状态枚举校验。
- station_code_identity 迁移在当前开发库已应用，用事务内编码锁串行同编码创建，避免 nullable 旧唯一键导致并发重复。新增/编辑及对应审计在同一事务中提交；同值重复编辑不会误报不存在，支持用空字符串清空地址/营业时间/电话。
- 后台站点页经纬度使用 InputNumber 发送数值，新增编辑、联系电话、营业时间、运营/停用/建设状态选择，提交中禁重复及错误显示。
- scripts/test-stations.ps1 真实接口通过：两个同编码并发创建仅一个成功，另一个 409；精度、非法坐标、编辑停用、清空字段及审计数量符合预期。前端生产构建通过，antd lint 仍提示既有静态 message 用法及 v6 对 v5 maskClosable 的兼容性警告；本轮尚未浏览器视觉验收。
- 未完成：站点角色权限/数据范围、完整分页搜索、计费/分账模板有效性与关联流程、删除引用检查及跨服务停用设备策略。当前接口停用站点后附近站点不展示，不等于已禁止该站所有设备启动。

### 站点角色权限（2026-09-26）

- 站点列表/详情、新建、编辑、删除分别校验 station.read/create/update/delete。每次请求读取当前账户与角色授权，禁用/软删除账户、软删除角色或撤回权限立即拒绝，不依赖 JWT 中旧权限列表。
- 新增权限目录迁移 0005；开发管理员初始化授予这四项权限，当前开发库已执行并保留原密码。其他角色不自动获得站点权限。
- 列表响应携带当前站点操作权限，页面据此启用新建/编辑按钮，刷新时重新获取授权；后端独立校验写操作。
- 真实接口回归通过：只读角色能读不能增改删，同一 JWT 在临时授予编辑权限后能编辑、撤回后 403，禁用账户和删除角色均立即 403；前端生产构建通过，antd lint 与上一轮同样三项警告。
- 本轮只覆盖角色操作权限；按站点分配的数据范围、删除业务引用检查、其他模块权限体系及全角色工作流仍未完成。

### 站点分页、搜索及浏览器验收（2026-09-26）

- 站点列表移除固定 200 条截断，支持 page/page_size（1..100）、keyword（编码/名称/地址的字面子串）、status。总数与当前页在同一数据库事务读取，按 id 倒序。
- PC 页面接入服务端分页、页大小、关键词及状态筛选/重置；筛选回第一页，保留请求代次校验防旧请求覆盖新结果。
- scripts/test-stations.ps1 使用 206 个有效站点验证第三页、总数、地址和状态筛选、软删除过滤、百分号字面匹配与非法参数，测试通过且数据清理完成。生产构建通过。
- 实际浏览器使用 22 个临时站点验证搜索唯一结果、重置与第二页剩余 2 条；发现并修复直达站点 URL 时侧栏仍高亮仪表盘的问题，侧栏改为跟随路由。临时站点及浏览器页已清理。截图保存在本机 visualizations/stations-pagination.png 和 stations-menu-selection.png。
- 站点数据范围、删除引用检查、计费/分账模板关联和跨服务停用策略仍待完成。

### 设备管理查询（2026-09-26）

- 设备列表改为服务端分页，默认 20 条、最多 100 条；支持设备编号/型号/站点名称和编码的字面子串搜索，以及管理状态、站点 ID、厂商 ID 筛选。计数与当前页在同一事务内读取，不再只取前 500 条。
- 列表、详情及设备订单入口实时校验 device.read；设备订单额外沿用 order.read，并强制使用路径中的设备编号，避免客户端查询参数改变范围。上游错误不再伪装为空订单列表。
- 后台设备页展示站点名称和管理状态、错误信息、分页与查询控件；导入按钮按返回的权限显示。管理状态不代表设备在线状态，实时端口、遥测及设备控制仍待完善。
- 新增 0006_device_read_permission.sql，开发种子授予 device.read。既有业务角色需按职责配置该权限。
- 验证：test-devices.ps1 的 505 台设备分页、筛选、软删除、权限撤销和设备订单隔离测试通过；后台构建及 antd lint 通过；浏览器验证第二页查询后回到第一页并正确显示单条结果。临时设备和订单已清理。

### 充电状态读取与页面（2026-09-26）

- 修复充电快照缓存命中绕过订单归属检查的问题：先在 user_db 检查当前用户、订单和软删除状态，再读 Redis；支持数字订单 ID 和订单号，缓存按规范订单号查找。
- 数据库状态决定 status/charge_state 和轮询；paid 保留等待启动状态，终态停止轮询。缓存只能提供白名单测量字段，不能覆盖订单身份、状态、轮询标志，也不会透出其他缓存字段。缓存缺失时测量为 null，不伪造零值；实时遥测与告警的完整 gateway 获取链路仍待接通。
- 停止充电入口在调用 gateway 前校验订单归属、软删除和 charging 状态，并改用严格内部 HTTP 客户端。真实 STOP 下发与 ACK 仍未完成，不能把现有 gateway 的占位响应视为物理设备已停止。
- ongoing 查询排除软删除并包含待支付订单，严格解码；首页区分待支付/等待启动/充电中，修复钱包 tabBar 导航。
- 重写已有 charging 页面：读取状态、显示缺失测量、定时刷新、终态停止、页面隐藏/退出取消轮询、忽略迟到响应、失败重试及订单详情入口。扫码/报价/支付确认仍在开发，未声称完整充电流程完成。
- 验证：17 项小程序 Node 测试通过；test-orders.ps1 新增缓存越权、已删除订单、数字 ID、旧缓存终态、paid 状态、测量缺失及非法停止请求测试并通过。尚未在微信开发者工具运行完整小程序。

### 扫码和端口选择页面（2026-09-26）

- 新增已在 app.json 注册但原先缺失的 scan 与 scan-result 页面。首页进入统一扫码入口，支持相机二维码和手动输入设备编号/端口码；取消扫码不报错，读取前按需登录，导航对端口码编码。
- 结果页消费现有 kind=port/device 协议，展示端口号与状态；设备码需选择空闲端口，选择时重新调用只读 scan/port，端口变为占用时及时禁用。直接端口码展示该端口。解析和选择不创建订单、不预占端口。
- 登录、加载、失败重试、页面隐藏后迟到响应隔离已接入；端口身份不匹配或响应结构异常会报错。当前仅支持后端认可的原始设备/端口码，未加入任意 URL 二维码解析。
- 当前仍缺站点计费信息及支付确认，页面明确提示无法取得计费信息，不以默认价格创建支付单。此处是未完成项，后续需接通真实规则报价、下单与支付。
- 小程序共 22 项 Node 测试通过；未在微信开发者工具或真机完成相机/支付测试。

### 站点规则报价（2026-09-26）

- 报价接口移除固定默认规则和“分钟 × 0.2 度”的计算。新增 admin 内部设备计费配置读取：设备必须启用且绑定运营中站点；优先当前生效的站点规则，无匹配时回退到站点模板的有效默认规则；多条当前规则报 409，未配置/已停用/过期规则报 404。通过内部 HTTP 读取，不跨库查表。
- billing 报价明确要求 estimated_kwh（字符串，最多三位小数，0.001–100 度）与 estimated_minutes（1–1440）。使用整数 Wh/分计算，按预计时长均匀分配电量、以 Asia/Shanghai 当前分钟匹配分时电价，支持跨午夜、kwh/minute/mixed 服务费及起步价。时段重叠、预计窗口缺失价格、非法费率均拒绝；起步价补足计入服务费，保证分项之和等于合计。
- 新增用户只读 scan/quote；返回规则版本、站点、分项、估算依据与五分钟报价到期时间。扫码结果页可编辑预计电量和分钟数、查看报价，编辑输入即失效旧报价。scan/start 请求也改为明确携带预计电量/分钟，重新计算，不能继续使用旧默认预估。支付确认页面尚未接入，该预览无支付/订单/锁副作用。
- 内部 HTTP 保留规则冲突与端口占用业务错误；内部鉴权/服务故障仍映射为服务不可用。
- 验证：billing 14 项、内部 HTTP 6 项、小程序 24 项测试通过；test-pricing.ps1 验证真实 gateway/admin/billing/user 路径、金额、起步价、时效、冲突、模板回退、停用资源、占用端口及无订单副作用，临时数据清理。
- 未完成：PC 计费编辑与完整权限审计、设备在线检查、功率分档、优惠券/余额抵扣、规则快照持久化、用户确认金额防漂移、真实支付以及最终计量结算。最终 calculate/Stream 仍有默认规则占位，报价改进不代表结算已完成。

### 报价确认与订单快照（2026-09-26）

- 用户 scan/quote 返回服务端生成的 quote_id，Redis 临时保存用户、端口、预计电量/时长、完整规则和分项金额。明确有效期最多五分钟，校验不依赖 Redis TTL 抖动。
- scan/start 必须提供 quote_id 和一致的端口及预计参数；先校验归属、过期和已使用记录，再从 billing 重算完整规则/分项。任何变化返回 409，用户需重新确认，不占端口、不创建订单。规则比较不限于总金额，版本或费率变化但总额不变也会拒绝。
- 新增 user_db/0004_charge_order_pricing.sql：订单/支付单/事件/确认报价快照同事务写入；quote_id 唯一约束防止重复创建订单。UUID 统一格式后检查和持久化，避免大小写别名绕过。快照保留站点、规则版本、时段与预估分项，供后续结算接入。
- 验证：真实 MySQL 测试确认重复报价约束（含大小写别名）及整笔事务回滚；test-pricing.ps1 增加客户端参数变更、他人报价、过期及费率变化时的拒绝测试，并验证无订单/端口预占副作用。
- 当前快照尚未接入正式结算；微信预下单失败后的恢复、可重试支付、前端支付确认和支付回调/设备启动仍待完善。本轮不声称真实支付链路已完成。

### 微信预下单签名（2026-09-26）

- 替换 JSAPI 预下单请求及 wx.requestPayment 参数的占位签名，使用 RSA-2048 / SHA256 / PKCS#1 v1.5，私钥签名启用随机盲化。请求先序列化一次并签名相同的 HTTP body 字节，签名覆盖方法、含 query 的路径、时间戳、随机串及报文。
- 预下单成功响应先验证固定配置的微信支付公钥 ID、五分钟时间偏差及原始响应 RSA 签名，再解析 prepay_id。缺失签名、错公钥、过期或正文改动均拒绝。当前实现采用微信支付公钥 PEM 模式，不自动下载/轮换平台证书。
- 新配置：WECHAT_MERCHANT_SERIAL_NO、WECHAT_PRIVATE_KEY_PATH、WECHAT_PLATFORM_KEY_ID、WECHAT_PLATFORM_PUBLIC_KEY_PATH。路径是服务进程内路径，容器部署须只读挂载对应文件。商户私钥与平台公钥用途不同，不可互换；示例未包含任何真实密钥。
- 充电下单和钱包充值在创建支付记录前校验签名配置；钱包充值同时修正 INSERT 取 ID。缺少密钥不再产生占位签名或新支付记录。
- 验证：common-wechat 6 项测试通过，覆盖真实生成的 2048 位 RSA 密钥、请求签名串、前端支付签名、正文篡改/过期/错误 key ID/缺失签名和缺失私钥。开发环境报价回归包含缺失支付配置时不创建订单/预占端口的检查。
- 仍未完成：微信回调验签和解密、正确入账及设备启动、预下单失败恢复、退款签名、商户线上联调。当前 refund/callback 仍有占位实现；不能据此宣布支付可上线。
- 官方参考：https://pay.wechatpay.cn/doc/v3/merchant/4012365339 （JSAPI 调起签名）；https://pay.wechatpay.cn/doc/v3/merchant/4013053249 （微信支付公钥验签）；https://pay.wechatpay.cn/doc/v3/merchant/4013053420 （响应原文与五分钟时间校验）。

### 微信付款通知与入账（2026-09-26）

- 回调改用原始 HTTP body 验证微信支付 RSA 签名、公钥 ID 和五分钟时间偏差；再使用原始 32 字节 APIv3 密钥、nonce、associated_data 解密 AES-256-GCM。校验通知类型、JSAPI/SUCCESS、商户/appid、币种、金额范围和付款时间。失败返回非 2xx，只有数据库提交成功才返回空的 HTTP 204。
- 支付记录与 payer.openid、金额、支付方式相互核验；不再信任 attach 或明文顶层字段。幂等摘要绑定支付单、微信交易号、用户、金额及付款时间。重复通知不重复入账、不重复写事件，也不把已退款支付单改回已支付；不同订单复用交易号拒绝。旧的缺少摘要的幂等记录不会直接确认，需要人工核对修复。
- 普通充电付款同事务更新 payment_order.paid_cents/status、charge_order.paid、时间线与 outbox；启动事件使用充电订单号并携带用户/设备/端口。取消、关闭、失败、过期或冻结用户无法启动的订单收到款项后，创建待退款记录与 refund_required 事件，不生成启动事件。
- 钱包充值同事务增加余额、记录唯一流水并关联支付单；按用户行锁串行创建首个钱包，冻结账户不自动解冻。入账按支付单标价 total（微信优惠前应付订单额），通知 payer_total 参与幂等一致性验证；尚未接入微信账单/优惠资金归属对账。
- user 服务自行发布本库 outbox，使用 SKIP LOCKED；Redis 发送失败保留 pending 并安排重试，成功才标记 published。崩溃窗口可能重复发布同一 event_id，消费者仍需幂等。数据库死锁进行有限整事务重试，失败后让微信重试通知。
- 验证：common-wechat 8 项测试；user 15 项测试（含真实 MySQL/Redis）通过，覆盖取消后付款、重复通知、金额/用户不符、交易号复用、已退款重放、五路并发充值、事务回滚及 Redis 失败后恢复。实际 HTTP 无签名请求返回 401。测试无真实微信调用、无真实商户凭证。
- 未完成：gateway 正确消费并下发真实启动指令、支付失败恢复与前端调起支付、退款签名及执行闭环、账单对账和商户线上联调。本项完成不代表整个支付/充电流程可上线。
- 官方参考：https://pay.wechatpay.cn/doc/v3/merchant/4012791861 （JSAPI 支付成功通知）。

### 设备启动结果持久化（2026-09-26）

- user 的 start-result 已移除跨 schema 查询 device_port 和端口 ID=0 兜底。使用共享 DTO，要求稳定 command_id、设备/端口身份，成功确认必须提供 gateway 数字端口 ID。
- 新增 user_db/0005_charge_start_receipt.sql（开发库已应用），持久化每笔订单的设备启动确认。重复确认不重写开始时间，晚到重放不复活已结束订单，相反结果/不同指令拒绝。
- 支付与充电状态检查、端口占用、订单状态、时间线及启动回执同事务提交；支付单优先加锁，死锁有限重试。活动端口已被另一未结束订单占用时拒绝，去除无条件 ON DUPLICATE KEY UPDATE 覆盖。
- 启动失败在同一事务建立待退款记录与 outbox；重复失败仅产生一笔，已有退款金额不能重复申请。提交后按真实印刷端口码释放本订单逻辑锁。
- 验证：真实 MySQL 测试涵盖成功/失败重放、不同指令和设备、缺少数字端口 ID、其他订单占用以及晚到成功；test-orders.ps1 的后台查询/权限/时间线/失败回调回归通过，测试数据清理。
- 网关的 START 发送、设备 ACK 关联、超时后的断电确认、Stream 重试恢复和真实 MQTT 仍待替换占位实现。本轮是用户服务接收端的修复，不表示设备已可真实启动。OpenAPI 生成源 services/user/src/openapi.rs 当前不存在，契约以共享 DTO 和本接口文档同步维护。

### Stream 重试和重启恢复（2026-09-26）

- common-stream 不再首次失败就写死信。首次执行后最多重试三次（2/4/8 秒），成功才 ACK；每轮先恢复同名消费者的未确认消息，再读取新消息。ACK 或死信写入失败时保留 PEL 并稍后恢复。
- 新消费组从 ID 0 建立，接收启动前已发布的事件；已有消费组进度不重置。当前恢复针对同名消费者，跨消费者失联接管尚未实现。
- 原子死信操作先核对 PEL 所属消费者，再保存完整 envelope、原始字段、来源消息/组/消费者和错误摘要，随后 ACK。写入失败不确认原消息，重复完成不重复增加死信。被裁剪的 PEL 保存缺失报文诊断，避免永久阻塞，但不能恢复已被裁剪的原文。
- 真实 Redis 集成测试覆盖启动前事件、重连恢复、短暂错误后成功、重试耗尽、完整死信内容、消费者身份不符、死信写入失败恢复和被裁剪的 PEL。
- 未完成：运维端死信查看/人工重放、跨消费者失联接管、gateway 持久化 START 命令和真实设备 ACK。消息基础设施不代表业务消费者中的占位逻辑已完成。
- 官方参考：https://redis.io/docs/latest/commands/xreadgroup/ ；https://redis.io/docs/latest/commands/xack/ 。

### 网关持久化启动与 TCP 确认（2026-09-26）

- 新增 gateway_db/0003_charge_command.sql（开发库已应用），持久化每订单唯一 START/STOP 指令、端口占用所有权、发送会话、确认状态和 user 回写结果。替换跨库 charge_order 查询、OTA 表假 START 和模拟 ACK。
- 启动事件通过严格内部 HTTP 核对订单/支付身份及 paid 状态，使用印刷端口码检查逻辑锁、数字端口 ID 持久化占用。已占用/停用/失锁/离线且尚未发送时拒绝启动；错误订单不释放原有占用。
- 实际通过 9100 行 JSON TCP 连接下发 START，仅正确设备/端口/会话/命令 ACK 能更新状态。user 持久化确认后才 ACK Stream；重复事件和重复 ACK 不重复启动或重写状态。
- 发送结果未知、20 秒 ACK 超时或 user 确认冲突转 STOP 补偿。STOP 使用独立稳定 UUID，设备确认停止之前不退款释放端口；迟到 START ACK 不取消补偿。恢复扫描采用游标，避免前排离线设备饿死后续指令。
- 端口启动占用对扫码暴露 reserved，小程序显示“启动处理中”且不可选。
- 验证：test-charge-start.ps1 全链路通过；附加 -RestartGateway 的真实容器重启测试也通过，证明不重复发送 START、重连后可完成 STOP。覆盖错误端口 ACK、付款状态等待真实 ACK、重复事件、设备拒绝退款、超时停止确认、逻辑锁丢失和既有占用保留。小程序回归同步验证 reserved 展示。
- 尚未完成：厂商真实协议/MQTT Broker、多网关连接路由、用户主动停止与计量结束、退款执行、异常设备的后台处置界面及实体设备联调。当前证据来自模拟设备和开发环境，不能声称整个真实充电业务已完成。

### 用户主动停止与实测结束（2026-09-26）

- 替换 gateway 的 stopped=true 占位；停止请求校验真实充电订单后建立唯一持久化 STOP，首次返回 accepted=true/stopped=false。user API 转发明确结果，完成后重放保持原指令。
- gateway_db/0004_charge_stop.sql、user_db/0006_charge_end_receipt.sql 已应用到开发库。STOP ACK 必须包含该订单实测 Wh/秒数/停止时间，缺失时不补零，不提前结束。断线与未确认时重发相同 UUID，禁止变更已确认读数。
- user 原子记录最终读数、completed、端口结束时间和回执；gateway 收到确认后释放自身占用并写 charge_ended outbox，由独立发布器重试发送。
- test-charge-start.ps1 -StopCharging 已通过真实用户会话 HTTP → gateway → TCP 模拟设备 → user 落库/快照全链路；覆盖受理状态、重复停止、最终电量、停止轮询和结束事件唯一性。真实 MySQL 测试验证读数不可变、错误启动/端口身份拒绝、重放不误释放下一订单占用。
- 当前结束订单金额仍待正式计费；billing 的旧结束消费者存在默认费率和跨 schema 占位，尚未完成实测数据+确认规则快照的结算。结束事件可靠保留以供后续处理，不能声称收费、差额退款或整套用户流程已经闭环。自然拔插头/充满结束、MQTT 和实体设备联调也未完成。
# 2026-09-26 实测计费接入进度

- 正式 calculate 与 charge_ended 消费共用实际计费入口，读取 user 的已完成订单、charge_end_receipt 和下单时 charge_order_pricing 快照；不再使用默认费率、60/40 峰谷分配、调用方传入电量或跨库更新订单。
- 电量采用整数 Wh，电费与服务费独立核算到分；分钟服务费按实测秒数折算，服务费合并后只舍入一次。支持当前 kwh/minute/mixed 模式和起步价。分时费率变化但缺少分段电量时返回待审核错误，不生成猜测账单。
- 新增 billing_db/0002_fee_receipt.sql，使用非分区订单唯一键串行化生成账单，并保存完整计量与规则来源快照；重放返回同一账单，来源快照变化拒绝覆盖历史金额。
- 新增 scripts/test-metered-fee.ps1，通过真实 HTTP/MySQL 验证保存费率、计量、所属用户、版本、重复请求及篡改来源拒绝；billing 17 项单测通过。
- 已接通实结费用回写：billing 的 fee_delivery 与费用记录同事务落库，每次失败延迟 30 秒重试；user 按支付订单→充电订单加锁，核对原始快照及价费之和，费用、幂等凭据、差额待退款与退款 outbox 同事务提交。重复通知无副作用，修改金额重放拒绝。
- 退款目标为已付金额减实结费用，再扣除已成功退款及待处理/失败退款占用额；退款总账不一致或已超退时拒绝自动登记。实结高于已付时保留实际费用并记录 shortfall_cents，尚无自动补扣或财务处理界面。
- 集成脚本验证首次投递失败不产生半成品、条件恢复后重试、100 分已付/18 分实结生成且只生成 82 分待退款、重复通知及篡改金额拒绝。待退款记录不代表微信已退到账。
- 尚未完成：微信差额退款执行、计量异常及欠款的财务审核页面、跨分时计量样本采集、功率分档/会员计费及后续分账。当前计费成功不代表以上后续流程已完成。
# 2026-09-26 微信退款基础协议与结果入账

- common-wechat 删除退款占位签名。退款申请与查询使用真实 RSA 请求签名、原文平台公钥验签、退款单/支付单/金额/币种绑定；请求缺省字段不传 null，金额非法、双支付标识、响应篡改或未知状态均拒绝。PROCESSING 保持处理中，不当作已到账。临时连接/服务错误按原退款单号重试。
- 协议依据：[微信支付退款申请官方文档](https://pay.wechatpay.cn/doc/v3/merchant/4012791862)。申请受理后仍需查询或通知确认退款最终结果。
- user 退款领取修复 payment_order_id 类型错误，支持同处理者幂等重试；其他处理者不得抢占。退款结果改为支付单→退款单锁顺序，成功仅累加一次，按新合计更新部分/全部退款，迟到失败不得覆盖成功。新增非分区 refund_success_receipt 防止同一微信退款 ID 被用于不同退款单；结果事件改为同事务 outbox。
- common-wechat 11 项测试通过，包含本地 HTTP 真实 RSA 请求验签与响应篡改测试；scripts/test-refund-result.ps1 使用真实 user HTTP/MySQL 验证领取、结果重放、部分/全部退款及迟到失败拒绝。
- 尚未接通 admin 持久化退款执行任务、PROCESSING 状态查询调度、退款通知和财务异常处理。此进度不代表线上微信退款闭环完成，测试未调用真实微信资金接口。
# 2026-09-26 持久化自动退款任务

- admin 的 refund_required 消费者改为先写 refund_task 后确认消息；退款单号全局唯一。queued/querying/reporting 阶段保存在 MySQL，重启继续处理，外部请求前固定退款参数。只在已验签的 RESOURCE_NOT_EXISTS 查询结果后使用原单原参数申请，PROCESSING 每分钟查询，成功后可靠回写 user；ABNORMAL/CLOSED 回写失败并转 manual_review。
- user 新增自动执行准备接口，只允许已验证的充电失败、关闭后迟到支付、实结差额退款。核对支付关联、微信交易号、已付金额及全部退款占用；钱包退款/人工领取/未知原因不进入自动执行。独立 refund_execution 记录系统领取，不伪造财务账号。
- 配置缺失或连接不确定保留任务重试，不写退款成功。开发环境配置仍为占位，未接入真实资金接口。
- 验证：admin 的 MySQL 持久任务 + 本地 RSA 签名 HTTP 模拟测试通过，覆盖查询不存在、申请处理中、查询成功、结果回写失败再试且申请次数为 1。真实计费集成脚本覆盖退款事件落任务、缺配置留队和 user 自动领取重放。
- 依据：[微信支付退款最佳实践](https://pay.wechatpay.cn/doc/v3/merchant/4014959631)。仍待完成：退款通知入口、查询频率长期退避、人工异常处置/双签审核页面、钱包退款拆分与对账。
# 2026-09-26 退款回调与查询并发

- 新增 user 退款公开回调，验签原文→AES-256-GCM 解密→商户、状态、支付身份及金额绑定→事务入账→HTTP 204。退款通知与查询共用 refund_result 核心，六路并发只累计一次退款、只写一次完成事件；错误金额拒绝，已成功后的已验签迟到异常通知仅确认收到。
- 微信配置新增可选 WECHAT_REFUND_NOTIFY_URL，admin 保存退款请求时带入；未设置使用商户平台退款通知配置。通知协议参考[微信支付官方退款通知说明](https://pay.wechatpay.cn/doc/v3/merchant/4012268885)。
- common-wechat 12 项测试通过（增加 RSA+AES 退款通知、金额/商户/状态/时间篡改）；user MySQL 并发回调/查询测试通过。尚未使用真实商户证书或资金接口联调。
- 后续仍需完成退款人工审核、异常处理页面、钱包原路退款拆分、对账、通知推送及其他 PC/角色业务流程。
# 2026-09-26 小程序扫码付款入口

- 扫码报价页加入明确的预付金额确认和 wx.requestPayment；报价过期不可创建订单，创建中重复点击不会重复下单。支付取消可在当前页面时限内沿用同一预支付参数继续支付，账号变化拒绝沿用其他会话参数。
- 下单响应不确定时冻结当前页面的再次创建入口，提供订单状态/列表核实；客户端成功仅进入服务端订单状态页，不伪造 paid/charging。支付窗口导致页面隐藏再显示时保留订单，成功导航延迟到页面恢复。
- 小程序 29 项测试通过，新增取消后同单重试、网络结果不确定不重复创建、报价过期/取消确认及支付窗口生命周期覆盖。
- 仍待完成：页面重建后的服务端预支付恢复、订单详情继续付款入口、真机微信开发者工具联调、优惠券/会员与钱包付款、其余后台角色流程。
# 2026-09-26 订单继续付款

- 新增 charge_prepay：创建订单时同事务保存完整预下单请求；首次扫码和订单恢复共用准备逻辑，保存微信 prepay_id 后重复使用。支付单→充电订单锁顺序与回调/取消保持一致，原支付单号、金额及付款期限固定。
- 订单详情新增继续付款。服务器验证用户/openid、待付款状态、支付关联、金额/商户配置、原到期时间和 Redis 端口预留，过期或预留丢失拒绝调起。响应增加 amount_cents，用户看到服务端预付金额后确认。
- 本地测试：真实 MySQL/Redis 验证缓存预支付恢复、重复使用同单及错误归属/已付/过期/预留丢失拒绝；小程序 30 项测试通过。尚未真机或真实微信预下单联调，旧的无 charge_prepay 快照订单不提供自动恢复。
- 仍需完成优惠券、会员/钱包充电、异常订单及退款审核、对账分账、其余 PC 角色流程。
# 2026-09-26 钱包原路退款事务

- 修复旧钱包退款先扣款再异步写 payment_order_id=0 的缺陷。现在按原充值支付时间拆分，每笔扣除已退、处理中和失败待核实占用；超过 365 天或退款次数已达 50 的来源不自动使用。
- 新增 wallet_refund_request/part，request_id（UUID）绑定用户、金额与原因；同请求重放不再预留余额。余额转入 frozen_cents、冻结流水、关联原支付单的退款及 outbox 同事务写入；退款执行核对预留凭据后复用持久任务，成功仅释放对应冻结资金一次。
- 五分钟内第三个有效请求冻结钱包并生成风控日志，返回 manual_review，不额外扣减资金；旧请求仍可幂等读取。失败/不确定结果保留预留资金，等待核实，不能在仍可能成功的退款上直接恢复可用余额。
- 测试：真实 MySQL 验证 200+300 两笔充值拆出 200+50、重复申请/结果不重复扣减、后续退款占用计算及第三次风控冻结。
- 当前执行复用已落地的持久退款任务，以保留网络中断后的状态；与早期文档同步调用退款的描述不同。尚需钱包申请页面、退款进度展示、财务人工解冻/异常处置及真实资金联调。

阶段补充：新增钱包退款冻结原因关联及独立解冻后端接口（user_db/0017、admin_db/0013）。需要先完成审核，由具备 finance.wallet_risk.release 的有效客户财务账号填写依据；只解除明确关联的退款频次冻结，其他冻结或用户冻结仍保留，余额及退款预留不变。审核队列支持 status=reviewed 查询历史。解冻按钮及审核历史 PC 交互尚未接入，本次作为开发中接口保存。
