# PC 后台与后端实现验收清单

目标：根据设计文档，完成全部 PC 后台、后端服务及充电用户小程序完整流程，包括设备导入及各角色日常工作的完整流程。能编译、HTTP 200 或空列表不能单独证明业务完成。

状态：开发中。以下各域均须完成真实持久化、参数校验、鉴权、异常处理、前端交互及对应集成测试，才可标记完成。

## 按角色贯通验收

| 角色 | 必须贯通的日常流程 | 当前证据与缺口 |
| --- | --- | --- |
| 充电用户（小程序） | 授权登录与协议同意 → 找桩/扫码 → 选择端口并展示价格 → 微信/余额/会员卡支付 → 设备确认启动 → 实时充电/主动停止 → 费用结算/退款 → 历史订单、发票、评价、报障、客服；余额充值、优惠券和会员卡管理 | 当前只有 index、charging、profile 三组页面；其余流程及完整异常补偿尚待实现和端到端验证 |
| 运营人员 | 站点配置 → 厂商/设备录入与批量导入 → 端口与二维码 → 费率/分账模板绑定 → 日常监控、订单排查、优惠券/会员卡/公告管理 | 设备页当前仅有列表；设备导入与错误行报告、重复处理、gateway 注册同步、审计均未完成 |
| 财务人员 | 账单核对 → 分账明细/汇总 → 提现审核与打款记录 → 双签退款 → 发票审核 → 对账差异处理 → 导出 | 订单分账只读已接入；各项财务写流程及权限隔离仍需逐项实现 |
| 巡检人员 | 设备状态/故障定位 → 告警确认 → 现场处理记录 → 恢复/关闭 → 查看历史 | 告警处置和可追溯处理流程尚未完整验收；不另建工单系统 |
| 客服坐席 | 微信原生客服接待 → 查询用户反馈/评价/投诉 → 回复与人工跟进 → 重大投诉关联告警 | 微信会话依赖配置与真实环境；后台留存、权限、脱敏和处理记录仍待完成 |
| 客户管理员 | 初始配置 → 账号与预置角色 → 功能/数据/字段权限 → 白标、客服坐席、SLA → 审计、Webhook、OTA | 不能以超级管理员验证替代各角色验收；每条流程须验证授权成功及越权被拒绝 |

上述表是全量目标的验收入口，不能用单个接口或局部订单页面通过代替角色流程通过。

| 业务域 | 依据 | 当前缺口与验收要求 |
| --- | --- | --- |
| 认证与账号 | api/admin.md A；需求 §4、§11 | 登录锁定、刷新令牌轮换/撤销、改密、个人信息、账号 CRUD、解锁、审计 |
| 角色与权限 | api/admin.md B；需求 §11.2 | 预置角色、操作权限、菜单权限、数据范围、字段脱敏；服务端拒绝越权 |
| 站点、厂商、设备 | api/admin.md C；api/gateway.md | 持久化 CRUD、筛选、关联限制、遥测/会话/故障详情、实时状态 |
| 订单查询 | api/admin.md D；api/user.md 历史/详情 | 真实跨服务分页、筛选、价费分离、分账详情、持久化事件时间线；禁止吞错为空列表 |
| 告警与风控 | api/admin.md E；需求 §10 | 规则、订阅、确认/关闭、限流阈值、触发及恢复、通知 |
| 财务审核 | api/admin.md F；api/billing.md | 分账单、双签退款、发票审核、对账及差异处理 |
| 优惠券与会员 | api/admin.md G；api/user.md | 模板、发放/使用统计、核销、有效期、并发幂等 |
| 公告、白标、客服、投诉 | api/admin.md H；需求 §5.8–5.10、§11 | 完整配置与发布、有效期、品牌应用、客服配置、反馈处理 |
| Webhook/监管推送 | api/admin.md I；需求 §10、§12 | 订阅、HMAC、SSRF 防护、投递、幂等重试、日志、人工重试 |
| OTA | api/admin.md J；api/gateway.md；api/worker.md | 固件校验、计划、执行、灰度、设备 ACK、双触发失败检测 |
| 计费与分账模板 | api/admin.md K；api/billing.md | 价费分离、分时规则、版本快照、总比例校验、整数舍入、幂等结算 |
| 审计与导出 | api/admin.md L；api/worker.md | 所有写操作审计、只读查询、导出任务、权限检查、过期下载 |
| 仪表盘 | 需求 §5；PC 后台 | 替换静态零值，真实指标、时间区间、趋势和异常状态 |
| 支付与充电闭环 | api/user.md；技术规格 §5 | 微信验签/解密、支付幂等、端口独占、设备 ACK 后启动、停止、退款与补偿 |
| TCP/MQTT 设备接入 | api/gateway.md；技术规格 §6 | 厂商适配、真实 MQTT、认证、心跳、指令 ACK、离线补传、遥测聚合 |
| 事件可靠性 | 技术规格 §5；api/worker.md | Outbox、PEL 恢复、消费者幂等、退避、DLQ 与人工重放 |
| 定时任务与运维接口 | api/worker.md | 文档任务码、任务记录/触发、跨服务访问、归档、对账、过期清理 |
| 公共基础与交付 | 技术规格 §7–§15 | 统一契约/错误、真实健康检查、迁移、OpenAPI、限流、日志指标、备份恢复、CI、部署验收 |

## 文档冲突处理

- `api/user.md` 的 P1-7 修正、`db/user.md` 和实际迁移均将 `charge_order` 放在 `user_db`；`api/admin.md` 的部分段落仍称其在 gateway，技术规格部分段落又称 billing。采用明确的 P1-7 修正：user 保存充电生命周期，billing 保存费用和分账，gateway 提供设备数据。禁止增加跨 schema SQL 来掩盖归属冲突。对外后台订单接口保持不变，内部查询由数据所有者提供。
- 现有运行环境使用 `X-Service-Token`，文档部分写 `Authorization: Bearer`。新客户端暂沿用现有内部鉴权，统一迁移与兼容测试仍列为公共基础待办。
- 数据库 SQL 与部分文档列名不一致；实现按已部署 schema 明确映射 API 字段，并随功能更新文档，不能用 `unwrap_or_default` 隐藏 SQL/反序列化错误。

## 已验证的基础修复

- Redis 消费者异步注册：两个真实 Redis 集成测试通过；五个后端健康接口返回 200。
- JWT 中间件与身份提取器兼容：三项回归测试通过；有效管理员可连续访问订单路由，未登录仍返回 401。

上述基础修复不代表业务域完成。每批新增实现须补充实际验证证据及剩余缺口。

## 2026-09-26 订单查询进展

- 已实现 user 所有的真实分页查询、时间/设备/状态筛选、admin 站点筛选及权限检查；前端包含筛选、分页、详情抽屉和失败重试。
- 详情通过内部 HTTP 读取 billing 费用及分账快照，展示参与方比例、分账金额、状态；最新计费结果优先于 user 旧费用快照。无记录与查询失败分别处理。
- `scripts/test-orders.ps1` 真实 MySQL/HTTP 集成测试通过，覆盖分页、软删除、校验、权限、支付退款、未计费、费用来源及两方分账；临时数据清理通过。
- admin-web 生产构建及 `cargo check --locked -p billing -p admin -p user` 通过。尚未完成本批浏览器视觉验收。
- 仍待完成：持久化事件时间线、订单数据范围权限、查询限流、列表费用快照同步，以及计费/分账写入闭环的完整实现与验收。上述查询进展不代表订单域或全局目标完成。

### 订单事件记录增量

- 新增 `user_db/0002_charge_event_log.sql`；本地已有数据库已应用，空库初始化按文件顺序执行全部迁移。其他已有环境需要先应用该增量 SQL，再部署依赖事件表的 user/admin 服务。
- 创建、取消、支付确认、设备启动结果与事件记录在同一事务内提交。设备重复启动结果不再重复改写状态；冲突结果返回 409。
- admin 时间线端点校验 `order.read`，user 排除软删除订单，按发生时间和主键稳定排序；前端订单详情展示实际事件。
- 真实接口测试已覆盖时间排序、软删除、越权、重复失败结果只写一条事件。完整事件链仍缺充电结束、结算、退款以及可靠事件消费；创建/支付外部闭环和浏览器验收尚未完成。

### 设备导入基础能力

- 新增 gateway 设备批量建档接口及共享 DTO，校验数量、ID、重复行、厂商和端口数。设备与端口、幂等记录同事务；重试不重建设备，冲突不覆盖配置。
- 新增 gateway_db/0002_device_provision.sql，本地已应用；独立非空主键解决导入请求之间的并发去重，未声称修复其他遗留写入口的可空唯一索引。
- `scripts/test-device-provision.ps1` 验证真实 HTTP/MySQL 创建、串行与并发重试、冲突、批次回滚、扫码解析和内部鉴权；共享契约测试通过。
- 尚未完成：PC 文件导入/预览/错误反馈、admin 站点与数据权限校验、元数据同步的持久化重试、审计、厂商管理及设备真实上线验收。设备导入全流程仍不能标记完成。

### 后台导入流程增量

- PC 设备页新增 UTF-8 CSV 模板、文件选择、校验预览、确认导入、最近 50 条个人导入记录及失败重试入口。
- admin 新增 device.import 功能权限检查、站点存在性检查、持久化导入请求、设备幂等保护；成功同步 gateway 后同事务提交后台元数据、完成状态及审计。失败保留错误，可在重启后人工重试。
- `admin_db/0002_device_import.sql` 已在本地应用。开发账户种子增加导入权限；生产角色授权仍待统一实现。
- 扩展真实集成测试通过：后台导入产生两侧设备、重复完成不重复审计、无效站点保留失败记录且无部分设备。前端构建通过。
- 仍待完成：浏览器操作验收、CSV 更全面测试、数据范围权限、后台自动重试、厂商管理及真实设备上线。当前版本使用厂商和站点 ID，尚未提供可选项查询和完整运营引导。

### 导入页面浏览器验收

- 已通过实际浏览器选择本地 CSV → 核对预览 → 确认导入 → 记录显示已完成 → 设备列表显示新设备的流程；临时设备、站点、厂商与审计数据已清理。
- 修复错误引号被静默改写、空行后错误行号不准确、非十进制数字被接受、重复选择同一文件无响应的问题。浏览器验证错误 CSV 显示具体行号并禁止提交。
- `admin-web npm test` 的 5 项 CSV 测试及生产构建通过，覆盖 BOM/CRLF、逗号/转义引号、坏引号、实际行号、重复设备、数值边界、控制字符和批次限制。测试运行需支持原生 TypeScript 加载的 Node 版本；开发容器为 Node 24。
- 仍需完成失败重试的浏览器验收、权限范围、自动同步重试、厂商配置和实际设备上线。局部导入验收不代表全部运营工作流程已完成。

### 导入自动恢复

- 新增 `admin_db/0003_device_import_retry.sql`，本地已应用。admin 每 5 秒恢复到期导入任务，临时服务故障限次退避，非法参数/冲突/撤销权限停止自动执行。所有尝试重新检查原操作员当前权限。
- 失败时回滚元数据到事务保存点，在持有任务锁时更新失败记录，避免并发失败覆盖完成状态。
- 单元测试验证退避和停止条件；真实集成测试模拟 gateway 已成功、admin 尚未提交的持久化任务，自动恢复后两侧各一条设备且审计完成。停用账号任务自动转为不可重试失败，gateway 无新设备。测试数据已清理。
- 自动恢复尚未经过真实断网/进程强杀及多实例负载测试；数据范围权限、厂商配置、完整用户充电闭环和其他角色业务仍未完成。

### 设备上线注册边界

- 删除注册接口中直接插入/覆盖设备的旧路径。注册现在仅接受预先建档且启用的设备，核对厂商、端口数及提供的站点/型号，并检查厂商状态和传输类型。
- 设备行锁保护会话更新：关闭旧会话、创建新会话、更新最近在线时间在同一事务提交；Redis 会话缓存失败不回滚数据库。
- TCP 演示连接禁止中途切换设备身份；处理失败返回错误，不再回成功 ACK。
- gateway 构建及真实 HTTP/MySQL 回归通过，覆盖重复注册不重复设备、旧会话关闭、未知/停用设备拒绝。TCP 身份切换也纳入集成脚本。
- 仍有明确缺口：注册请求目前保留既有 vendor_id/port_count 契约，尚未落实文档 vendor_code/protocol_version、会话令牌、私有协议版本协商及 TCP 注册会话一体化。厂商适配器和真正 MQTT 尚未实现，已向用户询问首批厂商型号与协议版本；其他模块继续推进，不将未知厂商能力标为完成。

### 充电用户订单流程

- 小程序新增我的订单、订单详情页面及个人中心入口；支持状态筛选、分页、登录提示、错误重试、刷新及防止旧请求覆盖当前页面。
- 用户历史查询增加总数、状态过滤、软删除过滤、严格分页参数校验；详情支持数值 ID 与原订单号，查询 SQL 绑定 JWT 用户 ID 后才访问 billing。用户响应不包含运营方分账参与者。
- 详情费用以 billing 最新结果为准；支付与退款摘要从 user 自有数据库读取。用户列表不再局限后台默认七天窗口。
- 真实接口测试验证本人查询、其他用户 404、软删除 404、计费结果与支付金额及无分账信息泄露。Node 页面逻辑测试 4 项、user Rust 单测 6 项通过。
- 尚未完成：站点名称关联、用户列表费用快照同步、文档其余详情字段、曲线/评价/发票链路，以及微信开发者工具实机验收。app.json 仍声明多个缺失页面，整包小程序尚不可标记可交付；不以页面逻辑测试替代微信运行验收。

### 用户找桩与站点详情

- 修复 user 未转发经纬度、未替换详情路径参数及吞下游错误的问题。admin 只返回开放且未删除站点，显式解码 DECIMAL 坐标，修复详情经纬度对调。
- 查询采用球面距离，按距离过滤并排序后限制最多 100 条，验证经纬度范围和 0.1–50 公里搜索半径。
- 新增小程序附近站点与详情页，包含用户主动定位、半径选择、地图标记、距离列表、导航和站点电话。定位失败、未登录及请求失败有明确反馈。
- 真实接口测试通过，覆盖经纬度转发、详情坐标、禁用站点排除、跨日界线距离与排序；参数单测和页面语法检查通过。
- 仍需完成空闲端口/在线状态、价格与设备摘要、微信地图实机验收、站点坐标系统一及小程序其余缺失页面。不可把站点基本信息查询当作完整找桩到充电闭环。

### 扫码读取契约修复（2026-09-26）

- gateway 扫码解析和端口详情统一使用印刷端口码字符串作为 `port_id`，设备扫码按端口号返回类型化列表。删除端口不可见，已停用/删除设备及厂商不可扫描；重复端口数据明确报冲突。
- user 扫码接口使用严格内部客户端，解包下游 envelope 后返回一次 envelope，404 保留为资源不存在，内部认证失败不清除用户登录。端口详情不再暴露其他用户的 `current_order_id`。
- 此处只实现只读设备/端口识别，未宣称在线可用性、价格、占用锁、支付和启动闭环已完成；小程序扫码与确认页仍待接入。
- 验证：gateway/user 的 cargo check --locked 通过；scripts/test-device-provision.ps1 通过，新增用户 JWT 转发、单层响应、端口码一致性、404、删除端口和禁用设备断言。

### 支付前订单数据修复（2026-09-26）

- scan/start 在预占端口前读取 gateway 的真实端口信息和 billing 报价，拒绝不存在/非空闲端口；优惠券抵扣尚未实现时明确报错，避免忽略选择后按原价支付。
- 修复原来 device_id=port_id、port_no=1 和 payment_order.total_cents=0 的写入错误。充电订单、支付订单关联及创建事件在同一事务内保存，预付款与最终计量费用分开。
- 验证报价总额为正、分项非负且求和一致、未溢出微信整数金额范围；事务写入失败释放自己的预占，提交后或提交结果不明则保留预占，等待恢复/到期。支付订单过期时间、微信请求和前端返回共用同一截止时刻。
- 单元测试与真实 MySQL 事务测试通过，覆盖金额溢出/不一致、7 号端口身份、非零支付额、订单关联、事件及回滚。
- 尚待完成：实际站点计费规则（当前 billing quote 仍为默认规则）、在线/实时占用复核、微信 RSA 签名和验签、支付创建失败恢复、取消/过期恢复和幂等重试。此改动不代表支付闭环可验收。

### 待支付取消校验与预占释放（2026-09-26）

- 取消接口按支付回调相同顺序锁定支付单、充电订单，校验用户归属、软删除、双向关联、两张单据状态及 60 秒窗口；不再只看充电订单而覆盖已支付单据，也不再用当月条件错误过滤跨月订单。
- 本地取消同时写 charge_order.cancelled / payment_order.closed 及一次取消事件。重放取消返回成功并重试 Redis CAS 释放，仅删除 `user_id:order_no` 匹配的预占，保留后来订单的锁。
- 真实 MySQL 测试覆盖他人订单、已支付单据、超时、重复取消及事件幂等；scripts/test-orders.ps1 真实 HTTP/Redis 回归通过，验证首次释放和后续预占不被误删。
- 未完成项：微信远程关单、取消先提交后迟到支付的退款补偿、可靠释放恢复任务。当前支付回调仍是待替换实现，不能据此宣称取消与真实支付的完整竞争处理已完成。

### 微信登录账户创建修复（2026-09-26）

- 修复 login 对 INSERT 使用 fetch_one 导致取不到用户 ID 的错误；新增 user_login_identity 主键锁表，按大小写精确的 OpenID 串行首次创建，避免旧 nullable 组合唯一键造成并发重复账户。迁移 0003 已应用本地开发库，新库初始化自动执行。
- is_new_user 根据实际创建分支返回，不再用最近 10 秒注册时间猜测。保存微信返回的 unionid；遇到重复历史账户或关联冲突报错，不自动合并、复活已删除账户或解冻账户。刷新现有 JWT 时检查用户当前状态及身份匹配。
- code2Session 使用结构化查询参数和有限超时，不把包含 app secret 的请求 URL 放进错误；兼容成功响应缺少 errcode/errmsg、失败响应缺少 openid，拒绝成功码但身份字段缺失的响应。
- 真实 MySQL 并发测试通过：同 OpenID 同时登录只有一次新建，后续登录沿用用户 ID，冻结/删除账户拒绝登录。微信响应解析单测通过。
- 仍待完成：微信真实 code 联调、Refresh Token 独立轮换与撤销、所有已签发令牌的实时禁用检查、登录审计及完整认证验收。

### 用户刷新令牌与小程序自动续期（2026-09-26）

- 登录返回独立随机 refresh_token（Redis 保存、7 天有效）及 jwt_expires_in；公开 refresh 改为 Authorization: Bearer RT_...，原访问 JWT 不再用于续期。轮换通过 Lua 原子消费旧令牌、保存新令牌，同令牌并发请求只有一次成功。
- refresh 查询当前账户状态，冻结/删除账户拒绝刷新并撤销该刷新令牌。新增 POST /api/v1/public/auth/logout，撤销提交的刷新令牌，重复调用可成功。
- 小程序保存刷新令牌，访问接口明确返回 401 时共用一个刷新请求并重试一次；403/业务失败不触发刷新，网络错误保留会话。退出等待本客户端在途刷新，成功撤销后清空本地会话；退出请求失败可重试，不假报服务端撤销成功。
- 验证：真实 Redis 并发测试、scripts/test-user-session.ps1 HTTP+DB+Redis 回归、小程序 session.test.cjs 3 项测试通过，覆盖轮换/重放/访问 JWT 拒绝/退出/冻结/并发续期/断网。
- 限制：退出当前只撤销刷新令牌，已签发访问 JWT 在过期前仍有效；会话族整体撤销和所有用户路由实时状态检查仍需补齐。JWT 时长沿用服务配置，尚未统一为设计的 15 分钟；微信实机联调尚未完成。

### 用户会话即时撤销与实时账户状态（2026-09-26）

- 用户 JWT 增加 sid，新登录及刷新签发的访问令牌固定 15 分钟。所有 /user 路由统一校验 Redis 会话存在、用户 ID/OpenID 匹配及数据库 active/未删除状态；无 sid 的旧用户令牌需重新登录，内部服务和后台认证不受此接口变更影响。
- Redis 维护同一登录会话当前刷新令牌。轮换保留旧令牌的会话关联直至原 TTL，只允许当前刷新令牌续期；退出可凭该会话已轮换的旧令牌撤销整组会话，解决退出与刷新竞争时漏撤销新令牌的问题。历史刷新令牌不会重新获得刷新能力。
- 实时 HTTP 回归通过：访问令牌可读取本人订单，使用刷新前的旧令牌退出后该访问令牌立即 401；冻结账户现有访问令牌立即 403，刷新拒绝并撤销会话。Redis 并发轮换测试通过；订单及设备集成测试使用真实测试账户和独立会话。
- 仍需微信实机登录与支付联调，以及用户身份审计、请求限流等设计验收项；这轮不代表全量目标已完成。

### 个人中心资料与登录状态（2026-09-26）

- /user/profile 改为类型化响应，在同一事务读取资料、未删除钱包、有效未使用优惠券数量及当前有效会员卡；返回注册时间、手机号是否已保存加密信息、可用/冻结余额，不返回 OpenID、电话明文或哈希。余额来自当前 schema 的 balance_cents（对应可用余额），保留旧 balance_cents 字段供现有调用兼容。
- 查询失败不再吞成零余额；钱包重复明确 409。已过期/使用/删除/禁用模板优惠券不计入可用数量，只有有效期内 active 会员卡返回。
- 个人中心未登录时提供直接微信登录；加载、错误、重试可见，资金固定两位小数，移除不存在的默认头像资源。隐藏页面或退出后迟到的资料请求不能恢复旧用户资料。
- scripts/test-user-session.ps1 真实接口新增个人中心断言通过；小程序 10 项测试通过，含 3 项个人中心异步状态测试。未完成微信开发者工具/实机视觉验收，手机号绑定、优惠券使用、钱包资金与会员交易仍需继续开发。

### 钱包余额与资金流水读取（2026-09-26）

- 钱包余额接口返回可用金额、冻结金额及状态，过滤删除账户；不存在账户为零，查询失败不伪装零余额，重复账户明确报冲突。
- 流水接口按 JWT 用户归属查询，支持设计中的 type 筛选（consume→pay、admin_adjust→adjust），时间/id 倒序稳定分页、总数及严格分页校验；返回带正负号的 amount_cents、txn_type、remark 和 RFC3339 时间，数据库解码错误不再用零值替代。
- 新增小程序钱包主页和流水页，支持登录、两位金额、冻结提示、类型选择、加载更多及失败重试；退出后在途读取不会回填旧流水。
- scripts/test-user-session.ps1 真实接口覆盖资金数据、消费筛选、分页、其他用户隔离及非法参数；小程序 13 项测试通过，含新增 3 项钱包测试。未完成微信实机视觉验收。
- 充值页/支付、余额消费、原路退款及对账闭环仍需继续开发；钱包写接口原有占位实现尚未完成，不能视为钱包全功能验收通过。

### PC 站点维护阻断修复（2026-09-26）

- 修复新建站点 INSERT/fetch_one 错误，改用 last_insert_id；修复 DECIMAL 经纬度读取为 0/详情解码失败。新增经纬度范围、文本长度/控制字符和状态枚举校验。
- station_code_identity 迁移在当前开发库已应用，用事务内编码锁串行同编码创建，避免 nullable 旧唯一键导致并发重复。新增/编辑及对应审计在同一事务中提交；同值重复编辑不会误报不存在，支持用空字符串清空地址/营业时间/电话。
- 后台站点页经纬度使用 InputNumber 发送数值，新增编辑、联系电话、营业时间、运营/停用/建设状态选择，提交中禁重复及错误显示。
- scripts/test-stations.ps1 真实接口通过：两个同编码并发创建仅一个成功，另一个 409；精度、非法坐标、编辑停用、清空字段及审计数量符合预期。前端生产构建通过，antd lint 仍提示既有静态 message 用法及 v6 对 v5 maskClosable 的兼容性警告；本轮尚未浏览器视觉验收。
- 未完成：站点角色权限/数据范围、完整分页搜索、计费/分账模板有效性与关联流程、删除引用检查及跨服务停用设备策略。当前接口停用站点后附近站点不展示，不等于已禁止该站所有设备启动。

### 站点角色权限（2026-09-26）

- 站点列表/详情、新建、编辑、删除分别校验 station.read/create/update/delete。每次请求读取当前账户与角色授权，禁用/软删除账户、软删除角色或撤回权限立即拒绝，不依赖 JWT 中旧权限列表。
- 新增权限目录迁移 0005；开发管理员初始化授予这四项权限，当前开发库已执行并保留原密码。其他角色不自动获得站点权限。
- 列表响应携带当前站点操作权限，页面据此启用新建/编辑按钮，刷新时重新获取授权；后端独立校验写操作。
- 真实接口回归通过：只读角色能读不能增改删，同一 JWT 在临时授予编辑权限后能编辑、撤回后 403，禁用账户和删除角色均立即 403；前端生产构建通过，antd lint 与上一轮同样三项警告。
- 本轮只覆盖角色操作权限；按站点分配的数据范围、删除业务引用检查、其他模块权限体系及全角色工作流仍未完成。

### 站点分页、搜索及浏览器验收（2026-09-26）

- 站点列表移除固定 200 条截断，支持 page/page_size（1..100）、keyword（编码/名称/地址的字面子串）、status。总数与当前页在同一数据库事务读取，按 id 倒序。
- PC 页面接入服务端分页、页大小、关键词及状态筛选/重置；筛选回第一页，保留请求代次校验防旧请求覆盖新结果。
- scripts/test-stations.ps1 使用 206 个有效站点验证第三页、总数、地址和状态筛选、软删除过滤、百分号字面匹配与非法参数，测试通过且数据清理完成。生产构建通过。
- 实际浏览器使用 22 个临时站点验证搜索唯一结果、重置与第二页剩余 2 条；发现并修复直达站点 URL 时侧栏仍高亮仪表盘的问题，侧栏改为跟随路由。临时站点及浏览器页已清理。截图保存在本机 visualizations/stations-pagination.png 和 stations-menu-selection.png。
- 站点数据范围、删除引用检查、计费/分账模板关联和跨服务停用策略仍待完成。
